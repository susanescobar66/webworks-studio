# Cedar & Stone Home Services — Week 5 Starter Project

## Project
**Week 5 — Build with a System: Bootstrap**

WebWorks Studio is creating a responsive service page for Cedar & Stone Home Services. The client content and visual direction are supplied. Your job is to implement the approved direction using **Bootstrap 5.3**.

## Before You Begin
1. Create `webworks-studio/week05-bootstrap/` inside your semester repository.
2. Place all starter files inside that folder.
3. Open the entire `week05-bootstrap` folder in Visual Studio Code.
4. Open `design-reference.svg` and review the approved client direction.
5. Preview `index.html` before editing.
6. Review the TODO comments in `index.html`.

## Bootstrap Setup
Bootstrap CSS and the Bootstrap JavaScript bundle are already connected in `index.html`.

**Do not replace the supplied Bootstrap CDN links with an older version from a tutorial.**

The starter uses Bootstrap **5.3.8**.

## Required Bootstrap Work

### 1. Bootstrap Responsive Grid
Use Bootstrap's `container → row → column` structure where appropriate. The page should respond intentionally at narrow, medium, and wide viewport widths.

### 2. Required Component: Responsive Navbar
Convert the supplied header/navigation into a Bootstrap **Navbar**.

The navigation must:
- display appropriately on larger screens;
- collapse at smaller viewport widths;
- use Bootstrap's collapse behavior;
- remain keyboard usable; and
- retain appropriate Bootstrap accessibility attributes.

### 3. Required Component: Cards
Convert the three service offerings into Bootstrap **Cards**.

The cards should:
- use a consistent structure;
- respond appropriately across viewport widths;
- include the supplied image, category, heading, description, and link; and
- align visually as a group.

### 4. Bootstrap Utilities
Use Bootstrap utilities where appropriate for spacing, display, alignment, sizing, gaps, borders, text, and buttons.

### 5. Limited Client-Specific CSS
Use `css/custom.css` only when the Cedar & Stone brand direction requires styling that Bootstrap does not reasonably provide.

Do not rebuild Bootstrap in your custom stylesheet.

## Optional Bootstrap Components
After completing the required work, you may use another Bootstrap component if it genuinely fits the interface. Possible examples include List Group, Badge, or Alert.

Additional components are **not required**.

## Preserve
Do not unnecessarily change the supplied client content, semantic HTML structure, meaningful image alternative text, section order, working links, or client contact information.

You may modify markup when necessary to implement correct Bootstrap component structure.

## Bootstrap Decisions
Before submitting, identify approximately three meaningful Bootstrap choices.

### Decision 1
**Used:**  
**Why:**  

### Decision 2
**Used:**  
**Why:**  

### Decision 3
**Used:**  
**Why:**  

## Bootstrap vs. Tailwind
Write approximately **75–100 words** answering:

**How did building this page with Bootstrap feel different from building with Tailwind in Week 4? Identify one situation where you might choose Bootstrap and one where you might choose Tailwind.**

[Replace this text with your response.]

## Live Site
[Add your published GitHub Pages URL here.]

## Final Reminder
The goal is not to use as many Bootstrap classes as possible.

**Choose the system → use the system correctly → adapt it to the client → test the result.**
